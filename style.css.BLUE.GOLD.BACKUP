:root{
  --bg:#0b1220;        /* tło strony (granat) */
  --panel:#0f172a;     /* karty/sekcje */
  --line:rgba(255,255,255,.10);
  --text:#e6e6e6;
  --muted:#aab1c2;
  --gold:#d4af37;      /* akcent złoty */
  --accent:#4db8ff;    /* link/hover subtelny */
  --maxw:1100px;
}

/* Reset + baza */
*{ box-sizing:border-box }
html{ scroll-behavior:smooth }
body{
  margin:0;
  background:var(--bg);
  color:var(--text);
  line-height:1.6;
  font-family:'Poppins',system-ui,Segoe UI,Roboto,Arial,sans-serif;
  -webkit-font-smoothing:antialiased; -moz-osx-font-smoothing:grayscale;
}
h1,h2,h3{ margin:0 0 .6rem }
h1,h2{ font-family:'Playfair Display',serif }
h2{ font-size:1.9rem; color:var(--gold) }
a{ color:var(--accent); text-decoration:none }
a:hover{ opacity:.9 }

section{ padding:3rem 1rem }
.container{ max-width:var(--maxw); margin:0 auto }

/* ---------- HERO (nowy) ---------- */
.hero{
  position:relative;
  isolation:isolate;
  min-height:clamp(280px, 45vh, 520px); /* kompaktowy */
  display:grid;
  align-items:end;
  padding-block:3rem 1.25rem;
}
.hero-media,
.hero-media img{
  position:absolute;
  inset:0;
  width:100%;
  height:100%;
}
.hero-media img{
  object-fit:cover;
  object-position:var(--focus-x,50%) var(--focus-y,42%); /* lekko wyżej */
}
.hero-overlay{
  content:"";
  position:absolute;
  inset:0;
  background:linear-gradient(to top,
    rgba(0,0,0,.55) 0%,
    rgba(0,0,0,.28) 40%,
    rgba(0,0,0,0) 68%);
  z-index:1;
}
.hero .hero-content{
  position:relative;
  z-index:2;
  color:#fff;
  text-shadow:0 2px 10px rgba(0,0,0,.35);
  max-width:var(--maxw);
  margin-inline:auto;
}
.hero h1{
  font-size:clamp(24px, 3.8vw, 40px);
  margin:0 0 .35rem 0;
  color:var(--gold);
  letter-spacing:.3px;
}
.hero p{
  font-size:clamp(14px, 2vw, 18px);
  margin:0 0 .9rem 0;
  color:var(--text);
  opacity:.95;
}
.hero-nav{
  display:flex; gap:.6rem; flex-wrap:wrap;
}
.hero-nav a{
  background:rgba(255,255,255,.14);
  color:#fff;
  padding:.45rem .75rem;
  border-radius:999px;
  backdrop-filter:blur(2px);
  transition:background .2s;
}
.hero-nav a:hover{ background:rgba(255,255,255,.22) }

/* Mobile hero */
@media (max-width:768px){
  .hero{
    min-height:auto;
    aspect-ratio:16/9;      /* stałe proporcje na tel. */
    padding-block:2rem 1rem;
  }
  .hero h1{ font-size:clamp(20px, 6vw, 28px) }
  .hero p{ font-size:14px }
  .hero-nav a{ padding:.38rem .6rem; font-size:14px }
}

/* ---------- Karty / sekcje ---------- */
.panel{
  background:var(--panel);
  border:1px solid var(--line);
  border-radius:14px;
  padding:1.2rem;
  box-shadow:0 10px 30px rgba(0,0,0,.25);
}
.features{
  list-style:none; padding-left:0; margin-top:.8rem;
  display:flex; gap:.6rem; flex-wrap:wrap;
}
.badge{
  display:inline-block;
  font-size:.78rem;
  padding:.15rem .55rem;
  border-radius:999px;
  font-weight:600;
  background:rgba(212,175,55,.15);
  border:1px solid rgba(212,175,55,.35);
  color:#f5e6b5;
}

/* ---------- Oferty ---------- */
#lista-lokali{
  display:grid;
  grid-template-columns:repeat(3,1fr);
  gap:1rem;
  margin-top:1rem;
}
@media (max-width:1000px){ #lista-lokali{ grid-template-columns:repeat(2,1fr) } }
@media (max-width:640px){  #lista-lokali{ grid-template-columns:1fr } }

#lista-lokali .lokal{
  background:var(--panel);
  border:1px solid var(--line);
  border-radius:16px;
  padding:1rem 1rem .8rem;
  transition:transform .18s, box-shadow .18s, border-color .18s;
}
#lista-lokali .lokal:hover{
  transform:translateY(-4px);
  box-shadow:0 16px 40px rgba(0,0,0,.35);
  border-color:rgba(255,255,255,.18);
}
#lista-lokali .lokal h3{ margin:.1rem 0 .4rem; font-size:1.1rem }
.kv{ display:grid; grid-template-columns:1fr 1fr; gap:.35rem .8rem; margin:.2rem 0 }
.kv p{ margin:0; color:#d8dbe6 }
.cena{
  margin:.6rem 0 .2rem;
  padding:.6rem .7rem;
  border:2px solid var(--gold);
  border-radius:10px;
  font-weight:700;
  background:rgba(212,175,55,.06);
}
/* statusy */
#lista-lokali .lokal .badge.badge-green  { background:rgba(16,185,129,.15); border:1px solid rgba(16,185,129,.4); color:#34d399 }
#lista-lokali .lokal .badge.badge-orange { background:rgba(245,158,11,.15); border:1px solid rgba(245,158,11,.4); color:#fbbf24 }
#lista-lokali .lokal .badge.badge-gray   { background:rgba(107,114,128,.15); border:1px solid rgba(107,114,128,.4); color:#9ca3af }

/* ---------- Galeria ---------- */
#galeria .grid{
  display:grid;
  grid-template-columns:repeat(auto-fit, minmax(260px, 1fr));
  gap:20px;
  margin-top:.8rem;
}
#galeria .grid a{
  display:block;
  border-radius:16px;
  overflow:hidden;
  border:1px solid var(--line);
  box-shadow:0 6px 18px rgba(0,0,0,.25);
  transition:transform .25s, box-shadow .25s;
}
#galeria .grid a:hover{
  transform:translateY(-2px);
  box-shadow:0 10px 28px rgba(0,0,0,.35);
}
#galeria img{
  width:100%;
  height:220px;
  object-fit:cover;
  display:block;
  filter:saturate(.95);
  transition:filter .25s;
}
#galeria img:hover{ filter:saturate(1) }

/* ---------- Rzuty ---------- */
#rzuty .rzuty-grid{
  display:grid;
  grid-template-columns:repeat(auto-fit,minmax(240px,1fr));
  gap:1.2rem;
  margin-top:1rem;
}
#rzuty .rzuty-grid a{
  display:block;
  text-align:center;
  background:var(--panel);
  border:1px solid var(--line);
  border-radius:12px;
  padding:1rem;
  box-shadow:0 4px 12px rgba(0,0,0,.1);
  transition:transform .2s;
}
#rzuty .rzuty-grid a:hover{ transform:scale(1.02) }
#rzuty img{
  width:100%; height:auto; max-height:260px; object-fit:contain;
  border-radius:8px; margin-bottom:.5rem;
}

/* ---------- Mapa ---------- */
#lokalizacja .map{
  border:1px solid var(--line);
  border-radius:12px;
  overflow:hidden;
  background:#0a0f1b;
}
#lokalizacja iframe{ width:100%; height:380px; border:0 }

/* ---------- Kontakt / Footer ---------- */
#kontakt .card{
  background:var(--panel);
  border:1px solid var(--line);
  border-radius:14px;
  padding:1rem;
}
#kontakt .muted{ color:var(--muted); font-size:.95rem; margin-top:.5rem }
.list{ margin:0; padding-left:1rem }

footer{
  border-top:1px solid var(--line);
  color:#cfd6e6;
  text-align:center;
  padding:1rem;
  margin-top:2rem;
}

/* ---------- Lightbox drobne ---------- */
.lb-data .lb-caption{
  color:var(--gold);
  font-family:'Poppins',sans-serif;
  font-size:1rem;
  margin-top:.5rem;
}
.lb-data .lb-number{ color:var(--muted); font-size:.85rem }
.lb-nav a.lb-prev, .lb-nav a.lb-next{ opacity:.8 }
.lb-nav a.lb-prev:hover, .lb-nav a.lb-next:hover{ opacity:1 }
.lb-close{ filter:invert(100%) sepia(30%) saturate(0%) hue-rotate(180deg) }

/* === HERO poprawki czytelności === */
.hero .hero-content {
  position: relative;
  z-index: 2;
  color: #fff;
  text-shadow: 0 3px 12px rgba(0,0,0,.6); /* mocniejszy cień */
  max-width: var(--maxw);
  margin-inline: auto;
}

/* Tło za tekstem (opcjonalne, tylko pod nagłówkiem i opisem) */
.hero h1,
.hero p {
  background: rgba(0,0,0,.35);
  display: inline-block;
  padding: .25em .6em;
  border-radius: 6px;
}

/* H1 lekko większe i pogrubione */
.hero h1 {
  font-size: clamp(24px, 3.8vw, 42px);
  margin: 0 0 .4rem 0;
  color: var(--gold);
  font-weight: 700;
}

/* Nawigacja hero – mocniejszy kontrast */
.hero-nav a {
  background: rgba(0,0,0,.55);  /* zamiast jasnego */
  color: #fff;
  font-weight: 500;
  text-decoration: none;
  padding: .5rem .85rem;
  border-radius: 999px;
  backdrop-filter: blur(2px);
  transition: background .2s;
}
.hero-nav a:hover {
  background: rgba(0,0,0,.7);
}
